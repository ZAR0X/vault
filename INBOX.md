# INBOX — Ram's messages for the next AI session

Anything Ram texts the Telegram bot that isn't a command (`apti <n>` / `status`) lands here with a timestamp. **Any AI (Claude Code or Gemini CLI) should read this at the start of a session, act on it, and then strike it through or note it handled.**

<!-- new messages are appended below this line by inbox.yml -->
